# Basis-URL des Cloudflare Workers (kein Secret, nur Konfiguration).
# Wird im Setup-Screen vorausgefuellt und kann dort ueberschrieben werden.
VITE_PROXY_URL=https://strava-kom-proxy.<account>.workers.dev
